# Third-party notices

The tracked `.tools/package-lock.json` records `@native-sdk/cli` 0.4.0 and its
platform runtime packages as Apache-2.0 licensed. The selected runtime package
is downloaded by npm for the host platform; this notice does not assert that
every optional platform package is bundled or distributed with Velocity.

For the authoritative license and source information, inspect the installed
package's `LICENSE` file, the
[npm package](https://www.npmjs.com/package/@native-sdk/cli/v/0.4.0), and its
[declared source repository](https://github.com/vercel-labs/zero-native).

Dependency lock metadata is evidence of installation inputs, not a substitute
for third-party license text.
