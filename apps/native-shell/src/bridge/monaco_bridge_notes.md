# Monaco Bridge Notes

## Now

Native placeholder with line-oriented fake code. No WebView in the app shell.

## Next milestone

- Embed Monaco in **one** isolated WebView (editor island only)
- Load after first native paint
- Keyboard routing + IME tests
- File open/save bridge
- Theme bridge
- Never put the whole shell in a WebView
