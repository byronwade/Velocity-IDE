# Feature Toggle Matrix

Lists registered features from `core/feature_registry.zig` / `feature_catalog.json`.
Allows enabling/disabling non-core features without restart (future); scaffold shows counts + sample rows.
